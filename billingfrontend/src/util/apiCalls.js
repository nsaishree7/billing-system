// import axios from "axios"
// import { PRODUCTS_URL } from "./url"

// export const getProductDataGST = (gstNo) => {
//     axios.get(PRODUCTS_URL + `?gstNo=${gstNo}`)
//       .then((data) => setProducts([...data.data]))
//       .catch(() => setProducts([]))
//   }